<!-- To generate html:
pandoc .\Exams\tenta_2026_03_14.md -o .\Exams\tenta.html --lua-filter=quickguide-generator/include-html.lua --standalone --css ../style.css
-->

<!-- 
**************************************************************************
Multiple choice questions: 14 points (can add 0,5 to each to push to 17.5) 
**************************************************************************
-->
<style>
body {
    max-width: none !important;
}
h1 {
    background-color: #2c2c2c;
    color: #ffffff;
    padding: 0.4em 0.7em;
    border-radius: 4px;
}
td pre {
    padding: 0;
    margin: 0;
}
td pre code {
    padding: 0.3em 0.5em;
    white-space: pre;
}
td:has(pre) {
    padding: 0.2rem 0.3rem;
}
</style>

NOTE: Unless explicitly asked in the question, you may assume that macro or structure definitions for ports and registers are available. No need to rewrite them for every assignment.

# Uppgift 1 (-0.5 ... 2.5p) 
<!-- (Alignment) -->

Givet följande variabeldeklarationer i C kod:

```
short b[5];
int c;
```

Välj det svar som i assembler-kod korrekt allokerar minnesutrymme för variablerna och förhindrar alignmentfel.

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
.align 2
a: .space 1
.align 1
b: .space 10
c: .space 4
```

</td>
<td>

```
a: .space 1
.align 1
b: .space 10
.align 1   
c: .space 4
```
</td>
<td>

```
a: .space 1
.align 2
b: .space 10
.align 1 
c: .space 4
```

</td>
<td>

```
.align 2
a: .space 1
b: .space 10
.align 2
c: .space 4
```

</td>
<td>

```
a: .space 1
b: .space 10
.align 1
c: .space 4
```

</td>
<td>

```
.align 1
a: .space 1
.align 2
b: .space 10
.align 1
c: .space 4
```

</td>
</tr>
</table>

<details><summary>Svar</summary>

<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
.align 2
a: .space 1
.align 1
b: .space 10
c: .space 4
```

</td>
<td>

```
a: .space 1
.align 1
b: .space 10
.align 1    #WRONG: need .align2
c: .space 4
```
</td>
<td>

```
a: .space 1
.align 2
b: .space 10
.align 1 <!--  WRONG: need .align 2-->
c: .space 4
```

</td>
<td>

```
.align 2
a: .space 1
b: .space 10 <!--  WRONG: need .align 1 before-->
.align 2
c: .space 4
```

</td>
<td>

```
a: .space 1
b: .space 10 <!--  WRONG: need .align 1 before-->
.align 1 <!--  WRONG: need .align 2-->
c: .space 4
```

</td>
<td>

```
.align 1
a: .space 1
.align 2
b: .space 10
.align 1 <!--  WRONG: need .align 2-->
c: .space 4
```

</td>
</tr>
</table>

</details>


# Uppgift 2 (-0.5 ... 2.5p)
<!-- (array offset calculation) -->

Följande variabler är deklarerade i C kod: 

```C
int i;
short arr[255];
```

och vi vill utföra följande tilldelningsoperation:

```C
arr[i+1] = 0;
```

Vilken assembler-kod nedan implementerar det korrekt?

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
lw   t0, i
la   t1, arr
slli t0, t0, 1      
addi t0, t0, 1      
add  t0, t0, t1
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 1   
slli t0, t0, 2       
add  t0, t0, t1     
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 2
slli t1, t1, 1       
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 1       
slli t0, t0, 1       
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
<td>

```
lw   t0, i
la   t1, arr
slli t0, t0, 1      
addi t0, t0, 1      
add  t0, t0, t1
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 2       
slli t0, t0, 1       
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
</tr>
</table>

<details><summary>Svar</summary>
<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
lw   t0, i
la   t1, arr
slli t0, t0, 1      
addi t0, t0, 1      <!--  WRONG: adds 1 byte, not 1 element-->
add  t0, t0, t1
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 1   
slli t0, t0, 2       <!--  WRONG: multiplies by 4-->
add  t0, t0, t1     
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 2
slli t1, t1, 1       <!--  WRONG: shifts &d-->
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 1       
slli t0, t0, 1       
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
<td>

```
lw   t0, i
la   t1, arr
slli t0, t0, 1      <!--  WRONG: multiplies by 4-->
addi t0, t0, 1      <!--  WRONG: adds 1 byte, not 1 element-->
add  t0, t0, t1
sh   zero, 0(t0)
```

</td>
<td>

```
lw   t0, i
la   t1, arr
addi t0, t0, 2       <!--  WRONG: increments by 2 elements-->
slli t0, t0, 1       
add  t0, t0, t1      
sh   zero, 0(t0) 
```

</td>
</tr>
</table>
</details>


# Uppgift 3 (-0.5 ... 2.5p) 
<!-- (Load-Store and types) -->

Följande variabler är deklarerade i C kod: 

```C
unsigned int k;
signed char a[5];
unsigned short s;
```

och vi vill utföra följande tilldelningsoperation:

```C
a[0] = k - s;
```

Vilket svarsalternativ nedan implementerar det korrekt i assemblerkod?

<!-- ERIK: Ändrade från "sbu" som inte finns till "sh" som också är fel-->

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
lw   t0, k
lh   t1, s
sub  t2, t0, t1
la   t3, a
sb   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lh   t1, s 
sub  t2, t0, t1
la   t3, a
sh   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lwu   t1, s 
sub  t2, t0, t1
la   t3, a
sw   t2, 0(t3)
```

</td>
<td>

```
lb   t0, k 
lhu  t1, s
sub  t2, t0, t1
la   t3, a
sw   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lhu  t1, s
sub  t2, t0, t1
la   t3, a
sb   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lh   t1, s 
sub  t2, t0, t1
la   t3, a
sh   t2, 0(t3)
```

</td>
</tr>
</table>

<details><summary>Svar</summary>
<table>
<tr>
<td>A</td><td>B</td><td>C</td><td>D</td><td>E</td><td>F</td>
</tr>
<tr>
<td>

```
lw   t0, k
lh   t1, s  <!-- WRONG: s is unsigned-->
sub  t2, t0, t1
la   t3, a
sb   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lh   t1, s  <!-- WRONG: s is unsigned-->
sub  t2, t0, t1
la   t3, a
sh   t2, 0(t3) <!-- WRONG: a is char-->
```

</td>
<td>

```
lw   t0, k
lwu   t1, s  <!-- WRONG: s is short-->
sub  t2, t0, t1
la   t3, a
sw   t2, 0(t3)  <!-- WRONG: a is char-->
```

</td>
<td>

```
lb   t0, k  <!-- WRONG: k is word-->
lhu  t1, s
sub  t2, t0, t1
la   t3, a
sw   t2, 0(t3) <!-- WRONG: a is char-->
```

</td>
<td>

```
lw   t0, k
lhu  t1, s
sub  t2, t0, t1
la   t3, a
sb   t2, 0(t3)
```

</td>
<td>

```
lw   t0, k
lh   t1, s  <!-- WRONG: s is unsigned-->
sub  t2, t0, t1
la   t3, a
sh   t2, 0(t3) <!-- WRONG: a is char-->
```

</td>
</tr>
</table>
</details>

# Uppgift 4 (-0.5 ... 2.5p) 
<!-- (C->ASM branches, loops) -->

Givet följande C kod: 

```C
int sum = 0;
int b;
int size;
short a[size];

for (int i = 0; i < size; i++) {
    if (a[i] < b) {
        sum += a[i];
    }
}
```
Vilken av assemblerimplementationerna nedan implementerar det korrekt? Anta att registren t0-t3 används så här:

```
li t0, 0          # sum = 0
li t1, 0          # i = 0
la t2, size
lw t2, 0(t2)      # t2 is size
la t3, b
lw t3, 0(t3)      # t3 is b
la t4, a  
```

<!-- ERIK: Didn't think this was needed? All comparisons are signed -->
Vilken assemblerkod nedan implementerar C koden korrekt?

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```
loop:
    blt  t1, t2, body  
    j end              

body:
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, skip    
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop
end:
```

</td>
<td>

```
loop:
    blt  t1, t2, body  
    j end              

body:
    lh   t6, 0(t4)       # t6 = a[i]
    blt  t6, t3, skip    
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop
end:
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```
loop:
    bge  t1, t2, end    
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, add     
    j continue    
add:
    add  t0, t0, t6      # sum += a[i]
continue:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop            
end:
```

</td>
<td>

```
loop:
    blt  t1, t2, end     
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, skip 
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop 
end:
```

</tr>
</table>

<details><summary>Svar</summary>
<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```
loop:
    blt  t1, t2, body  
    j end              
    
body:
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, skip    
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop
end:
```

</td>
<td>

```
loop:
    blt  t1, t2, body  
    j end              

body:
    lh   t6, 0(t4)       # t6 = a[i]
    blt  t6, t3, skip    <!-- WRONG: if (a[i] >= b) {sum +=} -->
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop
end:
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```
loop:
    bge  t1, t2, end    
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, add     <!-- WRONG: if (a[i] >= b) {sum +=} -->
    j continue    
add:
    add  t0, t0, t6      # sum += a[i]
continue:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop            
end:
```

</td>
<td>

```
loop:
    blt  t1, t2, end     <!-- WRONG: for (int i = 0; i >= size; i++) -->
    lh   t6, 0(t4)       # t6 = a[i]
    bge  t6, t3, skip 
    add  t0, t0, t6      # sum += a[i]
skip:
    addi t4, t4, 2       # a++
    addi t1, t1, 1       # i++
    j loop 
end:
```

</tr>
</table>
</details>

# Uppgift 5 (-0.5 ... 2.5p) 
<!-- (C->ASM ABI register use with functions, and a bit of stack) -->

You are given this C code

```C
int current_mA;
int load_mode;

int estimate_power_mW(int current_mA);
int calculate_factor(int load_mode, int base_power);
```

The following code should be implemmented:

```C
int compute_power_output(void) {
    int base_power_mW = estimate_power_mW(current_mA);
    int adjusted_power_mW = calculate_factor(load_mode, base_power_mW);
    return adjusted_power_mW * base_power_mW;
}
```

Select the RISC-V assembly implement that works correctly.

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```
compute_power_output:
    addi sp, sp, -8
    sw   ra, 0(sp)

    lw   a0, current_mA
    call estimate_power_mW
    sw   a0, 4(sp)

    lw   a1, load_mode 
    lw   a0, 4(sp)
    call calculate_factor

    lw   a1, 4(sp)
    mul  a0, a0, a1

    lw   ra, 0(sp)
    addi sp, sp, 8
    ret
```

</td>
<td>

```
compute_power_output:  
    lw   a0, current_mA
    call estimate_power_mW
    sw   a0, 0(sp)     

    lw   a0, load_mode
    lw   a1, 0(sp)
    call calculate_factor

    lw   a1, 0(sp)
    mul  a0, a0, a1

    ret
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```
compute_power_output:  
    lw   a0, current_mA
    call estimate_power_mW

    mv   s0, a0        
    lw   a0, load_mode
    mv   a1, s0
    call calculate_factor

    mul  a0, a0, s0
    ret

```

</td>
<td>

```
compute_power_output:
    addi    sp, sp, -8
    sw      ra, 0(sp)

    lw      a0, current_mA
    call    estimate_power_mW 
    sw      a0, 4(sp) 

    lw      a0, load_mode
    lw      a1, 4(sp)
    call    calculate_factor

    lw      a1, 4(sp)
    mul     a0, a0, a1

    lw      ra, 0(sp)
    addi    sp, sp, 8
    ret
```
</td>

</tr>
</table>

<details><summary>Svar</summary>

<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```
compute_power_output:
    addi sp, sp, -8
    sw   ra, 0(sp)

    lw   a0, current_mA
    call estimate_power_mW
    sw   a0, 4(sp)

    lw   a1, load_mode #WRONG: reverses the order of parameters
    lw   a0, 4(sp)
    call calculate_factor

    lw   a1, 4(sp)
    mul  a0, a0, a1

    lw   ra, 0(sp)
    addi sp, sp, 8
    ret
```

</td>
<td>

```
compute_power_output:   #WRONG: does not save ra
    lw   a0, current_mA
    call estimate_power_mW
    sw   a0, 0(sp)      #WRONG: rewrites last value in stack

    lw   a0, load_mode
    lw   a1, 0(sp)
    call calculate_factor

    lw   a1, 0(sp)
    mul  a0, a0, a1

    ret
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```
compute_power_output:   #WRONG: does not save ra
    lw   a0, current_mA
    call estimate_power_mW

    mv   s0, a0         #WRONG: does not save s0 before use
    lw   a0, load_mode
    mv   a1, s0
    call calculate_factor

    mul  a0, a0, s0
    ret

```

</td>
<td>

```
compute_power_output:
    addi    sp, sp, -8
    sw      ra, 0(sp)

    lw      a0, current_mA
    call    estimate_power_mW 
    sw      a0, 4(sp) 

    lw      a0, load_mode
    lw      a1, 4(sp)
    call    calculate_factor

    lw      a1, 4(sp)
    mul     a0, a0, a1

    lw      ra, 0(sp)
    addi    sp, sp, 8
    ret
```
</td>

</tr>
</table>


**Svar: D**

- `lw a0, current_mA` — loads `current_mA` into argument register `a0` as the first parameter ✓
- `sw a0, 4(sp) ` — stores `base_power_mW` into stack to reuse ✓
- `lw a0, load_mode` — loads `current_mA` as the first parameter for `calculate_factor` ✓
- `lw a1, 4(sp)` — loads `base_power_mW` as the second parameter for `calculate_factor` ✓
- after the function call, `lw a1, 4(sp)` — reloads `base_power_mW` from stack as it may have been corrupted in the function ✓
- `mul a0, a0, a1` — the return value is computed and stored int `s0` ✓

Alternativ A reverses parameters for `calculate_factor(load_mode, base_power_mW)`: it actually calls `calculate_factor(base_power_mW, load_mode)`.
Alternativ B does not save `ra` into stack, and rewrites last value in the stack
Alternativ C 

</details>

# Uppgift 6 (-0.5 ... 2.5p) 
<!-- (C pointers) mostly arithmetic -->
Vi vill implementera `apply_mask(int *array, int size, char mask)`-funktionen som applicerar en `mask` på **fjärde** byten (mest signifikanta byten) av varje element i `array`.

Att applicera en mask är en enkel bitvis `AND`-operation.

Välj det svarsalternativ nedan som implementerar denna funktion korrekt.

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++, array++) {
        char *byte_ptr = (char *)(array + 3); 
        *byte_ptr &= mask;
    }
}
```

</td>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++) {
        char *byte_ptr = (char *)(array + i);
        byte_ptr[3] &= mask;
    }
}
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++) {
        char *byte_ptr = (char *)(array + i * sizeof(int)); 
        byte_ptr[3] &= mask;
    }
}
```

</td>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    char *byte_ptr = (char *)array;
    for (int i = 0; i < size; i++, byte_ptr++) { 
        byte_ptr[3] &= mask;
    }
}
```
</td>

</tr>
</table>

<details><summary>Svar</summary>

<table>
<tr>
<td>A</td><td>B</td>
</tr>
<tr>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++, array++) {
        char *byte_ptr = (char *)(array + 3); //WRONG: array+3 jumps 3 elements forward
        *byte_ptr &= mask;
    }
}
```

</td>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++) {
        char *byte_ptr = (char *)(array + i);
        byte_ptr[3] &= mask;
    }
}
```

</td>
</tr>

<tr>
<td>C</td><td>D</td>
</tr>

<tr>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    for (int i = 0; i < size; i++) {
        char *byte_ptr = (char *)(array + i * sizeof(int)); //WRONG: pointer arithmetics jumps 4 bytes as it is int*
        byte_ptr[3] &= mask;
    }
}
```

</td>
<td>

```C
void apply_mask(int *array, int size, char mask) {
    char *byte_ptr = (char *)array;
    for (int i = 0; i < size; i++, byte_ptr++) { //WRONG: stepping through and modifying every byte
        byte_ptr[3] &= mask;
    }
}
```
</td>

</tr>
</table>


**Svar: B**

</details>

# Uppgift 7 (-0.5 ... 2p) (C pointers and parameters)

You are given the code below:

```C
void foo(int x, int *y) {
    x = x + 2;
    *y = *y + 3;
    y = &x;
    *y = 100;
}

int main() {
    int a = 5;
    int b = 5;

    foo(a, &b);

    printf("%d %d\n", a, b);
    return 0;
}
```

Select what will be printed by this code.

## Svarsalternativ

<table>
<tr>
<td>A</td><td>B</td><td>C</td>
</tr>
<tr>
<td>

```C
5 5
```

</td>
<td>

```C
7 8
```

</td>

<td>

```C
100 8
```

</td>

</tr>

<tr>
<td>D</td><td>E</td><td>F</td>
</tr>

<tr>
<td>

```C
5 100
```

</td>
<td>

```C
5 8
```
</td>
<td>

```C
100 5
```
</td>

</tr>
</table>

<details><summary>Svar</summary>

**Svar: E**

</details>

<!-- 
*******************************************************************
************* Open questions **************************************
*******************************************************************
-->

# Uppgift 8 (6p) 
<!-- (GPIO #define, GPIO config) -->

En knapp är kopplad till pin 5 i port D som visas på bilden nedan.

<img src="knapp.png" width="50%">


**a)** Skriv makrodefinitioner för registren CFGLR, OUTDR och INDR i GPIO port D. Skriv dessutom en makrodefinition för OUTDR_LOW som endast ger tillgång till OUTDR bitar [0..7]. (2p)


**b)** Vilket GPIO-läge bör väljas för pin 5 i port D för att på ett tillförlitligt sätt läsa av knappens status? Motivera ditt svar i en eller två meningar. (Tips: Du kan rita en diagram om det hjälper.) (2p)

**c)** Konfigurera pin 5 i GPIO port D i det läge du valde i del **b)**. Konfigurationen av andra pins får inte ändras. (2p)


# Uppgift 9 (7p) (SysTick config, CMPLR calculation, wait loop)

Erik: 
 * long long or uint64_t
 * Clarify which registers they can use? 
 * Is this question too simple? 

We want to write a function `long long int delay_micro(int micro_seconds)` that configures SysTick to count the provided number of microseconds and returns the number of clock cycles passed during that time.
We shall implement this function step by step in this uppgift.

Assume that system clock frequency is 144MHz.

**a)** Calculate the number of cycles that SysTick should count to measure the required number of microseconds. (1p)

**b)** Write a function `init_systick(int micro_seconds)` that configures and launches SysTick to count the provided number of `micro_seconds`. (2p)

**c)** Write a function `stop_systcik()` that pauses the clock (disables it without changing its configuration). (1p)

**c)** Write a function `long long int read_systicks()` that returns current value of SysTick counter. (1p)

**d)** Implement function `delay_micro(int micro_seconds)` that uses `init_systick(int micro_seconds)` to configure SysTick, blocks the program while SysTick is conting (waits untill delay happened), stops the clock with `stop_systcik()` and returns the number of ticks passed using `read_systicks()`. (2p)


# Uppgift 10 (4p) (Struct, union)

a) Needs clarification. Are dark gray non write/readable?
c) More points? 
d) 1p? 
(I think more points overall is fine)

Assume that we have a set of control and status registers for a peripheral communication device at the base address `0xE0008000`.
Its mane registers (CTRL, STATUS, and DATA) are placed according to the illustration below.

![](fake_port.png)

**a)** Write a struct-definition `PORT` that allows referencing all the registers. (1.5p)

**b)** Show how your stuct can be used in practice to write into `DATA` register. (1.5p)

**c)** Write a bit map struct which can be used to access individual fields in register `CTRL`. (0.5p)

**d)** Write a union that allows referencing `DATA_LOW` and `DATA_HIGH` fields separately as 16-bit values, as well as gives access to the whole 32-bit register `DATA`. (0.5p)

# Uppgift 11 (7p) (Interrupt, Vector table config, PFIC_IENx)

Erik: I don't think we have the USART in the quickguide. We should probably repeat a (possibly simplified) register overview here. Meybe give them the struct here?  
For c) we could perhaps do a fill in the blank, so they don't have to write the whole assembly file? 


We have two MD307 boards connected via USART1 module. One board gathers information from brightness sensors and sends it to the controller board which takes a decision whether to close or open blinds on an office building.

Your task is to program interrupts on the controller-board to read data from a sensor-board when it arrives.

Assume that USART1 module is initialised correctly and configured to produce an interrupt when data is received (RXNEIE bit --- RX not empty interrupt enable --- in register CTLR1 is set). Your task is to configure the interrupt on the controller board.

Assume that all the macro or struct defitions are given (use whatever you prefer).

**a)** Determine the interrupt number of USART1 interrupt. (1p)

**b)** Enable USART1 interrupt using the appropriate PFIC_IENx register. (1.5p)

**c)** Configure `mtvec` into vectored mode (mode `01`) and interrupt vector table so that `usart_isr()` is executed when USART1 interrupt happens.
Reminder: `.org <address>` directive allows you to position next instruction to a specific address in memory. (4p)

**d)** Implement interrupt handler `void usart_isr(void)` so that it reads a byte from `USART1->DATAR` and sets pin 0 at GPIO_D high if the read value is larger than a global int `threshold`. (0.5p)

# Uppgift 12 (7p) (EXTI, AFIO_EXTICR, EXTI ISR)

Erik: This is good, I think, but let's talk about how to correct it. 

We are developing a home alarm system. To secure doors, we decided to use a magnetic sensor that operates like a switch: when a door is closed the switch is closed as well, when a door is open the switch is open. Additionally, a vibration sensor is placed on the door to prevent robbers from cutting a hole in the door undetected. Electrically, vibration sensor also behaves like a switch: when movement happens the switch closes. (Imagine a wire suspended with unconductive springs in a conductive cover.)

![](larm.png)

As shown on the illustration above, the output of magnetic sensor is connected to pin 14 in GPIO port E, and output of vibration sensor is connected to pin 15.

Your task is to write a **complete** configuration code for MD307 board to work with the specified peripherals using external interrupts. When sensors detect an intrusion, a function `sound_alarm(int source)` should be called, where `source` is either `1` for magnetic sensor, or `2` for vibration sensor.

Assume that the board is used for other security components as well, so your configuration code should not affect any other components of the system.
Also assume that define statements or struct declarations for GPIO, EXTI, or PFIC registers are given. No need to declare all of them.

Tip: even if you cannot write the complete code or ran out of exam time, write down the plan of actions.

# Uppgift 13 (?p) (Assorted theory questions)

**a)** When GPIO pin is configured as output, what is the functionality of OUTDR register? (Short answer, one sentence) (1p)